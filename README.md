1. Создаем .env файл и оформяем его в следующем формате:

   ```
   BOT_TOKEN = "Токен от тг бота"
   API_URL=https://openlibrary.org/search.json
   COVERS_URL=https://covers.openlibrary.org/b/id

2. Выполняем установку необходимых библиотек при помощи команды:
   pip install -r requirements.txt

   
3. Запускаем main.py и пользуемся
